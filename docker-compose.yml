version: '3.8'

services:
  sprintflow:
    build: .
    container_name: sprintflow-app
    ports:
      - "3000:3000"
    volumes:
      # Syncs database file to local filesystem for persistent storage
      - ./db.json:/app/db.json
    restart: always
